# Changelog

## Unreleased
- Added `xtts_tts.py` to generate Coqui XTTS v2 audio locally.
- Added scripts to render video, generate XTTS audio, and mux audio into MOV.
- Added `REPRODUCE.md` with end-to-end steps and credits.
- Cleaned `outputs/` to keep only the final artifact.
